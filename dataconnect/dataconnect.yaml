specVersion: "v1beta"
serviceId: "webmatematica"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webmatematica-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
